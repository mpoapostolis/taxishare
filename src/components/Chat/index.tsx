import React, { Component } from "react";
import { chatContainer } from "./css";

class Chat extends Component {
  render() {
    return (
      <div className={chatContainer}>
        <h1>Chat</h1>
      </div>
    );
  }
}

export default Chat;
